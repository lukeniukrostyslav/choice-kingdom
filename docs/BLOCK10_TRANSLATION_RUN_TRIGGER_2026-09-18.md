# Block 10 translation run trigger

This marker intentionally triggers the resumable Block 10 narrative translation workflow on `main`.

The workflow is responsible for generating the seven non-English Core-8 narrative packs and committing each completed locale incrementally.
