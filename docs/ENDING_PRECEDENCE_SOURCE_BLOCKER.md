# Ending precedence source blocker

The canonical QA matrix requires explicit authored winners for simultaneous ending pairs. The repository currently exposes the required pairs but does not provide an authoritative source that selects the winner for those pairs.

This is intentionally recorded as a blocker rather than inferred from code order, enum order, event IDs, or narrative assumptions.

Block 5 cannot be declared 100% closed until the authored precedence data is supplied and the complete P01–P30 matrix is executable and GREEN.
