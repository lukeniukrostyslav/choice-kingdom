# Choice Kingdom — S06 Semantic Closure 01

Scope: E211–E270. Source inventory: `docs/SCENARIO_QA_S06_E211_E270_INVENTORY.md`.

## Closed semantic boundaries

### 1. Border-crisis producer boundary
E253 consumes the border-crisis condition; it is not a producer. The authoritative producer remains E271-A. This prevents a consumer from self-satisfying `pred.border_crisis`.

### 2. Coalition boundary
E261's `four_way_bargain` is not equivalent to `pred.coalition_cooperation`. E262 and E263 also cannot independently manufacture the qualified predicate. E264 is an explicit failure/withdrawal pressure node. E265 is a positive final-agreement candidate, but deterministic qualification still requires the independent coalition contract.

### 3. Constitutional qualification boundary
E256–E260 are stress-test choices, not automatic producers of `pred.constitutional_prepared_strong`. E209's final-charter prerequisites remain a multi-domain qualification contract and cannot be manufactured by any single late event.

### 4. Evidence boundary
E269 is a late consequence-stage evidence handoff distinct from E55. E270 combines two witness accounts but cannot by itself manufacture systemic-explanation verification or final-charter prerequisites.

### 5. Mara duplicate boundary
E226 is explicitly a late institutional-stress consequence and must remain distinct from E36's initial constitutional scene. Its condition references accumulated strain rather than merely repeating the earlier route.

### 6. Replay boundary
E247/E248 are replay-aware, but the current authored catalog does not establish exact `meta.*` producer/key contracts. No implicit cross-run state is allowed. This remains an S11 closure item.

## Open producer/consumer risks

- Several E211–E270 choices mutate numeric state without named fact outputs. The production contract must classify these as direct state mutations unless an explicit downstream predicate consumes them.
- E213, E221, E222, E228, E230, E236–E241 and several other branches contain meaningful state effects without canonical fact tokens. This is not declared an error yet because the engine schema is not frozen; it is a production-contract requirement.
- Delayed E242–E246 remain source-identified but lack exact lifecycle identities and exactly-once semantics.
- E264's low-coalition-trust trigger and E265's high-coalition-trust trigger require canonical predicate definitions; prose alone cannot become machine truth.

## Repository search checkpoint

Repository searches for the exact qualified tokens `pred.coalition_cooperation` and `pred.final_charter_prerequisites` returned no direct indexed matches at this checkpoint. This does not prove absence from every non-indexed source, so they remain contract-level QA targets rather than being declared missing producers solely from search results.

## S06 status

Direct inventory is complete. Semantic boundaries and hard negatives are recorded. Production-state normalization, delayed contracts, replay keys, predicate graph and reachability remain open.

**S06: 55% / IN PROGRESS.**
**Global Scenario QA remains 65%.**
