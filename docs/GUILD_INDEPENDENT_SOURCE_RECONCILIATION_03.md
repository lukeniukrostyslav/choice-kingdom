# Choice Kingdom — Guild Independent Source Reconciliation 03

Status: SOURCE-LEVEL QA — provisional closure pass, not engine implementation.

## Verified authored sources

The currently available authored expansion text confirms the following distinct guild institutional facts:

- E136-B: `roads_guild_contract` and immutable `history.guild_logistics_cooperation` are explicit outcomes of a guild transport decision. This is a qualified logistics/cooperation source and is not inferred from Ivo relationship.
- E144: `history.guild_representation` is explicitly established by both authored branches; the branches distinguish judicial separation/disclosure outcomes.
- E165: `official_credit_disclosure` / `private_credit_protected` are explicit credit-book outcomes. These are economic/credit facts, but must not be double-counted with another market/credit source unless the final contract defines them as distinct institutional domains.
- E168-A: `guild_tribunal_independent` is an explicit independent-tribunal fact. E168-B is explicitly guild-controlled and therefore does not satisfy the independent-tribunal domain.
- E124: `public_trade_standards` and `guild_trade_standards` are explicit commercial-standards outcomes. They are evidence of market regulation but are not automatically a separate institutional domain for `pred.guild_influence_strong` unless the final freeze explicitly allows that domain.

## Current qualification rule

`pred.guild_influence_strong` must be established from at least two distinct institutional domains. Relationship score alone is invalid. A downstream consumer cannot manufacture the predicate.

The strongest currently verified domain candidates are:

1. representation — E144 `history.guild_representation`;
2. independent tribunal — E168-A `guild_tribunal_independent`;
3. market/credit leverage — E165/E166 lineage, subject to exact domain-key freeze;
4. qualified logistics cooperation — E136-B `history.guild_logistics_cooperation`, later consumed by E194.

## Remaining P0 reconciliation

The original canonical catalog source for the earlier representation milestone must still be re-read and matched by exact event/choice ID before the domain is declared CLOSED. E124 standards also require anti-double-counting treatment. The final predicate producer must record exact source IDs and keys, not prose labels.

No engine schema is created from this provisional pass.
