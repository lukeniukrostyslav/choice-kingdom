# Choice Kingdom — Ending Qualification Canonicalization 02

Date: 2026-09-15
Status: **QA CLOSURE ARTIFACT — NOT ENGINE INPUT**

## Scope

This pass reconciles the ending qualification contract with the canonical producer/consumer registry. It does not invent missing producers and does not mark ending reachability as proven.

## Verified canonical replacements

| Stale design identifier | Canonical replacement | Evidence status |
|---|---|---|
| `thread.border` | `thread.border_crisis` | Canonical registry row exists; producer remains PARTIAL/OPEN for escalation semantics |
| `thread.guild` | `thread.ivo_market` for the canonical commercial route | Canonical registry row exists; route activation remains PARTIAL |
| `four_way_bargain` | `pred.coalition_cooperation` plus explicit coalition history such as `history.cross_faction_package` | No `four_way_bargain` producer found; do not create alias |
| `systemic_explanation_verified` | `pred.systemic_explanation_verified` | Canonical predicate namespace |

The ending contract has been updated so these stale identifiers are no longer treated as runtime names.

## Ending-family closure status

### Steward
Design identity is coherent, but runtime qualification remains blocked by unresolved producers for institutional reform, constitutional preparation and final charter prerequisites. Relationship values alone must never qualify Steward.

### Iron Crown
Canonical security/border vocabulary is now used. `thread.border_crisis` is still not a fully verified producer because the registry explicitly marks border escalation semantics OPEN. Military constitutional history has a strong candidate producer at E199-A, but the complete ending predicate is not yet frozen.

### Golden Compact
The generic `thread.guild` wording is removed. The canonical commercial route is `thread.ivo_market`; stronger guild influence must be represented by explicit predicates, not `rel.ivo`. `pred.guild_influence_strong` and `pred.guild_logistics_cooperation` remain OPEN in the registry.

### People's Charter
The contract permits an explicitly authored civic-governance route, but the exact canonical producer composition is not yet frozen. Public participation markers must remain durable authored history/flags rather than inferred from trust alone.

### Broken Diadem
No shortcut from low trust is allowed. A terminal failure predicate still requires a deterministic composition of independent unresolved crises and institutional/security/economic failure. This remains design/QA work, not runtime implementation.

### Quiet Throne
Explicit withdrawal/abdication history is required. Low power, low trust, relationship scores, or a weak final event must not qualify it.

### Second Founder
The contract now consumes canonical `pred.systemic_explanation_verified` and `pred.coalition_cooperation`. `history.cross_faction_package` is verified as an authored coalition input from E148-A. The systemic explanation predicate itself remains OPEN until the distinct evidence-set closure is frozen. Constitutional redesign and emergency-power expiry also remain producer-closure work.

## E265–E270 rules

1. E265–E270 are qualification/convergence nodes, not seven hidden ending resolvers.
2. E210 remains a convergence node only.
3. Ending identity must be selected from canonical predicates/history/routes after terminal validation.
4. Code order must not create implicit priority; priority belongs in authored data.
5. Replay metadata can qualify a current run only through an explicit `meta.*` transfer contract.
6. A graph edge is not a producer by itself.
7. A relationship is not a substitute for a route or durable history marker.

## Open producer-closure blockers

The following remain explicitly unresolved in the canonical registry and therefore block production schema freeze:

- `history.guild_representation`
- `pred.security_high`
- `pred.food_stable`
- `pred.transport_disruption`
- `pred.border_crisis`
- `pred.guild_logistics_cooperation`
- `pred.guild_influence_strong`
- `pred.systemic_explanation_verified`
- `pred.coalition_cooperation`
- `pred.constitutional_prepared_strong`
- `pred.budget_reform`
- `pred.final_charter_prerequisites`
- `pred.faction_routes_4`
- `thread.constitutional_late`
- `thread.final_constitutional_phase`

These are not to be resolved by arbitrary thresholds or aliases. Each needs an exact authored source, a deterministic definition, and downstream consumer verification.

## Required next pass

1. Build a unified E01–E270 producer/consumer table for every durable state key actually used by canonical events.
2. Freeze exact producers for the open rows above only where authored content supports them.
3. Define distinct evidence IDs for systemic explanation and a deterministic cardinality/set rule.
4. Define coalition cooperation independently from the number of active character routes.
5. Define border escalation independently from generic low security.
6. Define food stability as a predicate/marker, never as a sixth numeric resource.
7. Define guild influence/cooperation independently from `rel.ivo`.
8. Reconcile all legacy E35–E40 references and semantic duplicates E73/E156 and E99/E173.
9. Run graph/catalog reconciliation after the canonicalization pass.
10. Only after closure, compile the production data schema.

## Gate

**Ending qualification canonical naming: CLOSED for the identifiers audited in this pass.**

**Ending reachability: NOT PROVEN.**

**Production schema: BLOCKED.**

**Runtime engine: NOT STARTED by design.**
