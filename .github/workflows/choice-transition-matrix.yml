name: Choice Kingdom Choice Transition Matrix
on:
  push:
    branches: [main]
  pull_request:
jobs:
  choice-transition-matrix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate canonical choice transition matrix
        run: python3 tools/validate_choice_transition_matrix.py
